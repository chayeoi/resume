import { css } from '@emotion/core'

const global = css`
  a {
    color: inherit;
    text-decoration: none;
  }

  ul, ol {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  i {
    font-style: normal;
  }

  main {
    display: block;
  }
`

export default global
